apiVersion: v1 
kind: Service
metadata:
  name: test-service 
spec:
  selector:
    app: test
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30234
